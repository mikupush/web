import React from 'react';

const Features = () => {
  return (
    <section id="features" className="min-h-screen bg-white flex items-center justify-center">
      <h2 className="text-4xl text-gray-400">Aquí irá la siguiente sección ✨</h2>
    </section>
  );
};

export default Features;
